# 商丘工学院轮滑协会发展历程网站维护指南

本指南用于维护年份事件、活动图片、缩略图和预览图。

## 1. 文件说明

- `history-data.js`：页面配置、联系人、弹幕名单和发展历程数据。
- `index.html`：网页布局与交互，日常更新数据时一般不需要修改。
- `年份/`：存放该年份的原始图片，例如 `2014/16.png`。
- `年份/thumbs/`：网页时间线使用的小尺寸缩略图。
- `年份/previews/`：点击图片后全屏查看的压缩预览图。
- `optimize-images.js`：自动生成缩略图、预览图和优化后的首屏背景。
- `背景.png`：首屏背景原图。
- `背景.webp`：网页实际加载的优化背景。

网页只在点击“下载原图”时访问年份目录中的原始图片。时间线加载 `thumbs`，全屏预览加载 `previews`。

## 2. 只修改文字时

如果只修改以下内容，不需要重新生成图片：

- 年度标题、年度简介和会长信息。
- 事件标题、事件介绍和日期。
- 图片的 `alt` 或 `caption`。
- 图片显示顺序。
- 联系方式或弹幕名单。

直接修改 `history-data.js`，保存后提交到 GitHub 即可。

## 3. 新增活动图片

### 第一步：放入原图

将原图放入对应年份文件夹。例如：

```text
2026/
  1.jpg
  2.jpg
```

文件名必须与 `history-data.js` 中的 `file` 完全一致，包括扩展名和大小写。

### 第二步：配置事件

在 `history-data.js` 对应年份的 `events` 数组中增加事件：

```js
{
  month: 10,
  day: 23,
  title: "活动标题",
  description: "活动介绍",
  images: [
    {
      file: "1.jpg",
      alt: "图片内容说明",
      caption: "图片底部标题"
    },
    {
      file: "2.jpg",
      alt: "第二张图片说明",
      caption: "第二张图片标题"
    }
  ]
}
```

注意事项：

- 图片路径不需要写年份，页面会自动组合成 `年份/文件名`。
- 无法确认月份时，删除 `month` 和 `day` 两行。
- 有月份但没有具体日期时，只保留 `month`。
- 每个事件节点最多放 8 张图片；超过 8 张应拆成多个事件节点。
- 图片说明可以修改，但不要随意修改已经存在的文件名。

### 第三步：生成网页图片

第一次在这台电脑运行时执行：

```bash
npm install
npm run optimize-images
```

以后新增或替换原图，只需要执行：

```bash
npm run optimize-images
```

脚本会自动扫描所有四位数字命名的年份文件夹，并生成：

```text
年份/thumbs/原文件名.webp
年份/previews/原文件名.webp
```

例如原图为 `2014/16.png`，会生成：

```text
2014/thumbs/16.png.webp
2014/previews/16.png.webp
```

不要手动修改 `thumbs` 和 `previews` 中的图片。原图有变化时，重新运行命令即可覆盖生成。

## 4. 替换已有图片

如果保持原文件名不变：

1. 用新图片替换年份目录中的原图。
2. 运行 `npm run optimize-images`。
3. 将原图、缩略图和预览图一起提交。

如果更换了文件名，还需要同步修改 `history-data.js` 中对应的 `file`。

## 5. 删除图片

删除图片时需要同时处理：

1. 从 `history-data.js` 的 `images` 数组中删除对应图片对象。
2. 删除年份目录中的原图。
3. 删除 `thumbs` 和 `previews` 中对应的 `.webp` 文件。

如果一个事件删除图片后已经没有任何内容，可以将整个事件对象删除。

## 6. 修改首屏背景

将新的背景图片命名为 `背景.png` 并替换原文件，然后运行：

```bash
npm run optimize-images
```

脚本会重新生成网页使用的 `背景.webp`。

如需使用其他背景文件，可修改 `history-data.js`：

```js
heroBackground: "背景.webp",
```

## 7. 发布到 GitHub Pages

提交前确认包含以下内容：

- 修改后的 `history-data.js`。
- 新增或替换的年份原图。
- 对应年份的 `thumbs` 文件。
- 对应年份的 `previews` 文件。
- 修改背景时生成的 `背景.webp`。

常用命令：

```bash
git add .
git commit -m "更新协会发展历程"
git push
```

推送完成后，等待 GitHub Pages 部署。浏览器仍显示旧内容时，可以强制刷新页面或清理浏览器缓存。

## 8. 常见问题

### 新图片在网页中不显示

依次检查：

1. 原图是否放在正确的年份文件夹。
2. `file` 是否与真实文件名完全一致。
3. 是否运行了 `npm run optimize-images`。
4. `thumbs` 和 `previews` 中是否生成了对应文件。
5. 新文件是否已经提交并推送到 GitHub。

### 修改文字后是否需要运行图片命令

不需要。只有新增、替换原图或修改首屏背景时才需要运行。

### 为什么保留原图

时间线和全屏预览都使用压缩图片以提升加载速度，只有“下载原图”按钮需要原始文件。

### `npm install` 是否每次都需要执行

不需要。通常只在首次使用、更换电脑或删除 `node_modules` 后执行一次。

## 9. 更新检查清单

- [ ] 原图已放入正确年份文件夹。
- [ ] `history-data.js` 已添加或更新图片配置。
- [ ] 每个事件节点不超过 8 张图片。
- [ ] 新增或替换图片后已运行 `npm run optimize-images`。
- [ ] 时间线缩略图可以正常显示。
- [ ] 全屏预览加载的是压缩预览图。
- [ ] “下载原图”可以下载原始文件。
- [ ] 所有新增文件已提交并推送到 GitHub。
